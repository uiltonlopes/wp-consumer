<%= form_with(model: namespace, local: true) do |form| %>
  <% if namespace.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(namespace.errors.count, "error") %> prohibited this namespace from being saved:</h2>

      <ul>
        <% namespace.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :namespace %>
    <%= form.text_field :namespace %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
